# Pylint configuration for uDocket - MAXIMUM STRICTNESS
# All checks enabled except for specific project conflicts

[tool.pylint.main]
# Python version
py-version = "3.12"
# Ensure workspace packages are discoverable without installation
init-hook = "import sys; from pathlib import Path; ROOT = Path(__file__).resolve().parents[1]; sys.path[:0] = [str(ROOT / 'apps/api/src'), str(ROOT / 'apps/celery/src'), str(ROOT / 'apps/web/src'), str(ROOT / 'packages/udocket_domain/src'), str(ROOT / 'packages/udocket_ai_core/src'), str(ROOT / 'packages/udocket_utils/src'), str(ROOT / 'packages/udocket_worker_core/src')]"

# Files or directories to analyze
recursive = true

# Ignore patterns
ignore-patterns = [
    "^\\..*",
    "^__pycache__$",
    "\\.pyc$",
    ".*\\.egg-info$",
    "^build$",
    "^dist$",
    "^tests$",  # JUSTIFIED: pytest suites do not require docstring enforcement
    "test_.*\\.py$",  # JUSTIFIED: allow expressive pytest names without docstrings
    ".*_test\\.py$",  # JUSTIFIED: allow expressive pytest names without docstrings
    "^alembic$",  # JUSTIFIED: Alembic autogenerates migration modules
]

# Load all available plugins
load-plugins = [
    "pylint.extensions.check_elif",
    "pylint.extensions.bad_builtin",
    "pylint.extensions.docparams",
    "pylint.extensions.for_any_all",
    "pylint.extensions.set_membership",
    "pylint.extensions.code_style",
    "pylint.extensions.overlapping_exceptions",
    "pylint.extensions.typing",
    "pylint.extensions.redefined_variable_type",
    "pylint.extensions.comparison_placement",
    "pylint.extensions.mccabe",
    "pylint.extensions.confusing_elif",
    "pylint.extensions.consider_refactoring_into_while_condition",
]

# Enable all messages by default
enable = "all"

[tool.pylint.messages_control]
# Minimal disable list - only genuine conflicts with justifications
disable = [
    "line-too-long",  # JUSTIFIED: Ruff handles line length enforcement
    "bad-indentation",  # JUSTIFIED: Ruff handles indentation
    "too-few-public-methods",  # JUSTIFIED: Pydantic models are data classes with few methods
    "no-self-argument",  # JUSTIFIED: Pydantic validators use cls as first argument
    "redefined-outer-name",  # JUSTIFIED: pytest fixtures intentionally shadow outer names
    "duplicate-code",  # JUSTIFIED: High false positive rate, better suited for periodic scans
    "missing-module-docstring",  # JUSTIFIED: Docstring rules handled by Ruff with Google style
    "missing-class-docstring",  # JUSTIFIED: Docstring rules handled by Ruff with Google style
    "missing-function-docstring",  # JUSTIFIED: Docstring rules handled by Ruff with Google style
]

# Enable additional checks
enable = [
    "use-symbolic-message-instead",
    "useless-suppression",
]

[tool.pylint.format]
# Maximum line length (matches Ruff)
max-line-length = 120

# Expected format of line ending
expected-line-ending-format = "LF"

# Allow comments to be slightly longer
ignore-long-lines = "^\\s*(# )?<?https?://\\S+>?$"

# Maximum number of characters on a single line
max-module-lines = 1000

[tool.pylint.design]
# Maximum complexity (McCabe)
max-complexity = 10

# Maximum arguments
max-args = 7

# Maximum local variables
max-locals = 15

# Maximum return statements
max-returns = 6

# Maximum branches
max-branches = 12

# Maximum statements
max-statements = 50

# Maximum parents for a class
max-parents = 7

# Maximum attributes for a class
max-attributes = 8

# Minimum public methods for a class
min-public-methods = 0

# Maximum public methods for a class
max-public-methods = 20

# Maximum boolean expressions in an if statement
max-bool-expr = 5

[tool.pylint.basic]
# Naming conventions (strict)
argument-naming-style = "snake_case"
attr-naming-style = "snake_case"
class-attribute-naming-style = "any"
class-const-naming-style = "UPPER_CASE"
class-naming-style = "PascalCase"
const-naming-style = "UPPER_CASE"
function-naming-style = "snake_case"
inlinevar-naming-style = "any"
method-naming-style = "snake_case"
module-naming-style = "snake_case"
variable-naming-style = "snake_case"

# Docstring requirements
docstring-min-length = 10
no-docstring-rgx = "^_"

# Good variable names
good-names = ["i", "j", "k", "ex", "db", "id", "pk", "_", "e"]

# Include type hints in docstrings
accept-no-param-doc = false
accept-no-raise-doc = false
accept-no-return-doc = false
accept-no-yields-doc = false

[tool.pylint.imports]
# Check for deprecated imports
deprecated-modules = ["optparse", "tkinter.tix"]

# Allow wildcard imports from modules
allow-wildcard-with-all = false

# Analyse import fallback blocks
analyse-fallback-blocks = true

[tool.pylint.exceptions]
# Exceptions that trigger raise-without-traceback-from
overgeneral-exceptions = ["builtins.BaseException", "builtins.Exception"]

[tool.pylint.logging]
# Logging modules to check
logging-modules = ["logging"]

# Logging format style
logging-format-style = "new"

[tool.pylint.refactoring]
# Maximum nested blocks for function / method body
max-nested-blocks = 5

# Never returns functions
never-returning-functions = ["sys.exit", "argparse.parse_error"]

[tool.pylint.similarities]
# Minimum lines for duplicate code detection (strict)
min-similarity-lines = 4

# Ignore comments and docstrings
ignore-comments = true
ignore-docstrings = true


[tool.pylint.typecheck]
# List of decorators that produce context managers
contextmanager-decorators = ["contextlib.contextmanager"]

# List of members which are set dynamically
generated-members = []

# Ignore missing members on these
ignored-modules = []

# Ignore mixin classes
ignored-classes = ["SQLObject", "optparse.Values", "thread._local", "_thread._local"]

# Mixin class names
mixin-class-rgx = ".*MixIn"

[tool.pylint.variables]
# Check for unused arguments
allowed-redefined-builtins = []

# Dummy variable regex
dummy-variables-rgx = "_.*|dummy.*"

# Ignore unused arguments if names begin with underscore
ignored-argument-names = "_.*"

# Allow init-import
init-import = false

[tool.pylint.classes]
# Check for method parameters
defining-attr-methods = ["__init__", "__new__", "setUp", "asyncSetUp", "__post_init__"]

# List of method names that should always exist
exclude-protected = ["_asdict", "_fields", "_replace", "_source", "_make"]

# Valid metaclass names
valid-classmethod-first-arg = ["cls"]
valid-metaclass-classmethod-first-arg = ["mcs"]

[tool.pylint.string]
# String quote preference
check-quote-consistency = true

# Multiline string preference
check-str-concat-over-line-jumps = true
