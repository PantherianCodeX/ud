[project]
name = "udocket-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    # Task queue
    "celery>=5.5.3,<6.0.0",

    # Workspace packages
    "py-domain",
    "py-worker-core",
]

[tool.uv]
dev-dependencies = [
    # Dev dependencies inherited from root workspace
    # Package-specific dev deps can be added here if needed
]

[tool.uv.sources]
py-domain = { workspace = true }
py-worker-core = { workspace = true }
