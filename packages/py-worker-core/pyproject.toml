[project]
name = "py-worker-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "celery>=5.5.3,<6.0.0",
]

[tool.uv]
dev-dependencies = [
    # Dev dependencies inherited from root workspace
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/py_worker_core"]
