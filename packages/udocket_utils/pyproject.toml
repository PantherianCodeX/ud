[project]
name = "udocket-utils"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []


[tool.uv]
dev-dependencies = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/udocket_utils"]
include = ["src/udocket_utils/py.typed"]

[tool.hatch.build.targets.sdist]
include = ["src/udocket_utils/py.typed"]
